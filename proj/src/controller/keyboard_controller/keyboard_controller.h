#ifndef KEYBOARD_CONTROLLER_H
#define KEYBOARD_CONTROLLER_H

/**
 * @brief Updates the game state when the SPACE key is pressed
 * and draws the pause menu.
 * 
 */
void (pause_handler)();

/**
 * @brief Moves the pacman acccording to the pressed arrow key.
 * 
 */
void (process_key)();

#endif
